{
  "name": "osinachi-portfolio-gallery-lambda",
  "version": "1.0.0",
  "description": "Lambda function for monitoring and serving gallery configuration",
  "main": "index.js",
  "scripts": {
    "deploy": "serverless deploy",
    "local": "serverless invoke local"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0"
  },
  "devDependencies": {
    "serverless": "^3.0.0"
  }
}
